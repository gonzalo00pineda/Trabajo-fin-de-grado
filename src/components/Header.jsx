
// Este archivo define un componente funcional de React llamado Header.
// Este componente representa el encabezado de la aplicación y contiene un título.

const Header = () => {
    return (
        <header style={{ padding: "1rem", backgroundColor: "#eee" }}>
        <h1>Planificador de Novelas</h1>
        </header>
    );
};

export default Header;
// El componente se exporta lo que permite importarlo y usarlo en otros archivos.